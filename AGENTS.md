# Mars Cambridge 开发规则

本文件适用于所有开发者，以及在本仓库工作的 ChatGPT/Codex 任务。

## 产品范围

只开发 KET、PET、FCE。不要为 CAE 或 CPE 新增题库、页面或营销承诺。

## 开始任务前

1. 阅读 `README.md`、`docs/PRODUCT_SPEC_AND_HANDOFF.md` 和与任务相关的文档。
2. 运行 `git status --short`，不得覆盖他人的未提交修改。
3. 从最新主分支新建短期功能分支；不要直接向主分支提交。
4. 在 Issue/任务说明中写清级别、模块、试卷、Part、数据来源和验收条件。

## 题库单一来源

- 同一道题只维护一份结构化数据；专项练习和整套模考必须读取同一数据源。
- 每套题必须记录 `level`、`collection`、`book/test`、`paper`、`part`、`source file/page`、`answer source`、`verification status`。
- 未经核对的数据不得显示“官方”“已完成”或进入正式模考。
- 题干、选项、答案、图片、音频、写作提示和口语材料必须逐项对应。
- 不得根据常识猜答案或用生成内容伪装成官方答案。

## KET 基准交互

- 专项与模考使用相同题型组件和反馈逻辑。
- 模考必须按正式 Paper/Part 顺序运行，并有可暂停计时、进度保存、交卷、分 Part 得分、错题查看和只重做错题。
- 错题只在重新答对后进入“已掌握”；拼写错误也进入错题/复习队列。
- 阅读选择文本可查中文释义并加入生词库；单击不触发，拖选不完整单词时自动扩展到完整词。按钮、输入框等交互区域不得误触。
- 生词库可删除、复习，并提供“中英文版”和“默写版”打印。
- 写作保存草稿并提供与题目对应的参考内容；口语参考答案必须逐题对应，并体现“直接回答 + 理由 + 例子/细节”的 A2/B1/B2 层级要求。

## 完成定义

任务只有同时满足以下条件才算完成：

1. 数据来源可追溯且人工核对；
2. 专项与模考入口均能使用；
3. 正常作答、改答案、暂停/继续、刷新恢复、交卷、看答案、只重做错题均验证；
4. 桌面端和窄屏无阻断性布局问题；
5. `npm run audit:reading`、`npm run audit:words`、`npm run lint`、`npm run build` 通过；
6. PR 写明改动、测试、已知限制、截图和版权来源；
7. 不提交密钥、账号、个人数据、构建产物或 `node_modules`。

## 禁止事项

- 不复制一份题目数据来快速做专项页面。
- 不在没有授权确认的情况下公开发布考试扫描件或音频。
- 不把 ChatGPT 对话当作项目备份或唯一需求记录。
- 不共享个人 OpenAI/GitHub/API 密钥。
- 不将仅保存在 `localStorage` 的功能描述成支持跨设备同步。
